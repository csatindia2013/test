services:
  - type: web
    name: easybill-admin-dashboard
    env: python
    runtime: python-3.11
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
